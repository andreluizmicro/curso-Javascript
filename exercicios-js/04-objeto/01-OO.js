// CÓDIGO NÃO EXECUTAVEL!!!!!

// Procedural
processamento(valor1, valor2, valor3)

// OO
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        // ...
    }
}

objeto.processamento() // Foco passou a ser o objeto

/**
 * !! Principios imporantes:
 *
 * * 1. abstração -> Pegar um objeto do mundo real e traduzir para o dentro do seu sistema
 * * 2. encapsulamento -> Ter os detalhes de implementação escondidos
 * * 3.Herança -> Recebe atributos e comportamentos do Pai. JavaScript não permite Herança múltipla, só pode ter um único pai
 * * 4. Polimorfismo(multiplas formas).
 */
